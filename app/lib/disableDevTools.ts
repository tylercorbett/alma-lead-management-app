"use client";

if (typeof window !== "undefined" && process.env.NODE_ENV === "development") {
  // @ts-ignore
  window.__NEXT_DATA__ = {
    props: { pageProps: {} },
    page: "",
    query: {},
    buildId: "",
  };
}
